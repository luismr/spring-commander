package dev.luismachadoreis.blueprint.cqs.command;

/**
 * Interface representing a command in the Command-Query Separation (CQS) pattern.
 * Commands are used to perform actions that change the state of the system.
 *
 * @param <T> The type of the result returned by the command execution
 */
public interface Command<T> {} 